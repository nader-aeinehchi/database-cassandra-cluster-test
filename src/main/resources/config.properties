cluster.name=MyCluster
#cluster.size=5
hosts=127.0.0.1,127.0.0.2,127.0.0.3,127.0.0.4,127.0.0.5

number.of.rows.to.insert=50

#The test application fills with a payload consisting of UUID's.  It means every UUID is 16 Bytes.
#The size of payload * 16 in bytes.  E.g. 64 becomes 64*16=1024 meaning 1KB.
payload.size=512

#The number of threads that are used to create load
number.of.threads=5

